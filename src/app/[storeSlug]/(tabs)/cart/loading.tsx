import { StorefrontTabLoading } from '@/components/storefront/storefront-tab-loading';

export default function CartLoading() {
  return <StorefrontTabLoading label="Preparando sua sacola…" titleWidth="short" rows={3} />;
}
